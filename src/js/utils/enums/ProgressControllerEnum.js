const ProgressControllerEnum = Object.freeze({
    // Jump over 1 second of the track.
    SMALL_JUMP: 1,
    // Jump over 10 seconds of the track.
    BIG_JUMP: 10,
    // Jump over 25 senconds of the track.
    HUGE_JUMP: 25
});

export default ProgressControllerEnum;
