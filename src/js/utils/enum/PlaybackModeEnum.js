/**
 * This enum is used to contain the value of the different possile playbacks in manazeak.
 * @type {Readonly<{RANDOM: number, SHUFFLE: number, NORMAL: number}>}
 */
const PlaybackModeEnum = Object.freeze({
    NORMAL: 0,
    SHUFFLE: 1,
    RANDOM: 2
});


export default PlaybackModeEnum;
